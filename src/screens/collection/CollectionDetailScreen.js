import React from "react";
import { View, Text, StyleSheet } from "react-native";

const CollectionDetailScreen = () => {
  return (
    <View>
      <Text>CollectionDetailScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default CollectionDetailScreen;
