import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  ScrollView,
  Button
} from "react-native";

const ArtEditScreen = () => {};

const styles = StyleSheet.create({});

export default ArtEditScreen;
