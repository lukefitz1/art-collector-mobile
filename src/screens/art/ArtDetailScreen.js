import React from "react";
import { View, Text, StyleSheet } from "react-native";

const ArtDetailScreen = () => {
  return (
    <View>
      <Text>ArtDetailScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default ArtDetailScreen;
